import socket from './socket';

export default {
    socket,
    router: {
        history: {
            location: {}
        },
        match: {
            params: {},
        },
        routes: []
    }
}
